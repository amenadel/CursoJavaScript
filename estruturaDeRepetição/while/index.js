                    //ESTRUTURAS DE REPETIÇÃO
                    
                    // A ideia é repetir uma ação até atingir uma condição;

                    // Ao invés de repetirmos o mesmo código várias vezes, criamos um statment que fará uma checagem em cada loop;

                    //ESTRUTURA DE REPETIÇÃO: WHILE

                    //Fará uma ação, até que a condição seja atingida

                    //Precisamos realmente 'definir um fim' para loop, para não termos o problema de loop infinito

                    let x = 10 ;

                    while(x > 0) 
                    {
                        consnole.log(x);

                        x = x - 1
                    }

                    let y = 0;

                    while(y <= 10)
                    {
                        console.log(y)

                        y = y + 1
                    }


                    let z = 10;

                    while(z > 0)
                    {
                        console.log(z)

                        z = z + 1
                    }
                    
